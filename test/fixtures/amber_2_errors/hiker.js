'use strict';

module.exports = {
  answer
};

function answer() {
  // Two identifiers nothing reads, so jshint counts two errors rather than
  // one and its summary line is pluralised.
  const sixNines = 6 * 9;
  const sixSevens = 6 * 7;
  return 42;
}
