'use strict';

module.exports = {
  answer
};

function answer() {
  // The learner left a working note behind. jshint is configured with
  // "unused": true, so an identifier nothing reads is an error, and an error
  // stops cyber-dojo.sh before node is reached.
  const sixNines = 6 * 9;
  return 6 * 7;
}
