'use strict';

const hiker = require('./hiker.js');
const assert = require('assert');

// Half written. jshint lints every *.js in the sandbox, so this file is an
// error even before node decides which one of them to run.
assert.equal(String(hiker.answer()).length,

console.log('answerSizeTest ran');
